<div id="budgets" class="card card--list card--list--budgets">
  <% budgets.each do |budget| %>
    <% next if highlighted.include?(budget) %>

    <%= cell("decidim/budgets/budget_list_item", budget) %>
  <% end %>
</div>
