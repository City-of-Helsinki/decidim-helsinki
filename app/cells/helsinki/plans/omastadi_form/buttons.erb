<% if user_signed_in? %>
  <div class="actions text-center">
    <% if plan && plan.persisted? && plan.draft? %>
      <% # TODO: Show destroy draft plan %>
    <% end %>
    <% if plan && (!plan.persisted? || plan.draft?) %>
      <% # TODO: Show save as draft %>
      <%#= form.submit t(".buttons.save"), name: "save", class: "button hollow primary large mr-2", data: { disable: true } %>
    <% end %>
    <%= form.submit t(".buttons.create"), class: "button large primary mt-s mb-none", data: { disable: true } %>
  </div>
<% else %>
  <%= render :sign_in_box %>
<% end %>
