<% resources.each do |project| %>
  <p>
    <%= link_to resource_locator([project.budget, project]).path do %>
      #<%= project.id %> <%= translated_attribute(project.title) %>
    <% end %>
  </p>
<% end %>
